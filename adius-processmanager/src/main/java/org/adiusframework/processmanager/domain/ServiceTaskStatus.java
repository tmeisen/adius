package org.adiusframework.processmanager.domain;

/**
 * Enumeration for the different statuses which a ServiceTask could have: open,
 * closed, error.
 */
public enum ServiceTaskStatus {
	OPEN, CLOSED, ERROR;
}
