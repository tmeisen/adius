package org.adiusframework.processmanager.domain;

/**
 * Enumeration for the different statuses which a ServiceSubTask could have:
 * open, closed, waiting.
 */
public enum ServiceSubTaskStatus {
	OPEN, CLOSED, WAITING;
}
